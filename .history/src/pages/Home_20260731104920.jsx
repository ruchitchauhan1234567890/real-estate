import React from 'react'

const Home = () => {

  // let employees = JSON.parse(localStorage.getItem('employee')) || [];

  // // 2. Filter out the employee you want to delete (e.g., by id 101)
  // const empIdToDelete = "ronald";
  // const updatedEmployees = employees.filter(emp => emp.name !== empIdToDelete);

  // // 3. Save the updated array back to localStorage
  // localStorage.setItem('employee', JSON.stringify(updatedEmployees));

  return (
    <div className="border-2">
      hello this is a home page
    </div>
  )
}

export default Home
